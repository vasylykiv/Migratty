{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Migratty",
  "type": "object",
  "properties": {
    "init": {
      "type": "object",
      "properties": {
        "baseDir": {
          "type": "string",
          "description": "Input directory (where to get files from)"
        },
        "outdir": {
          "type": "string",
          "description": "Output directory (where to put the result)"
        }
      }
    }
  }
}
